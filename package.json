{
  "name": "typeform-mcp-server",
  "version": "1.0.0",
  "description": "MCP server wrapping the Typeform API for ClickUp Brain",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}